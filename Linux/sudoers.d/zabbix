#Defaults:zabbix !requiretty   # Uncomment in case of sudo errors

zabbix ALL=NOPASSWD: /usr/sbin/smartctl --scan, /usr/sbin/smartctl --scan -d nvme, /usr/sbin/smartctl -a /dev/*, /usr/sbin/smartctl -A -i -n standby /dev/*
